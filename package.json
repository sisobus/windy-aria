{
  "name": "windy-aria",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "windy-lang 코드를 음악으로 출력하는 sonification 도구",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "sync:examples": "rm -f src/examples/*.wnd && cp ../windy/examples/*.wnd src/examples/ && echo 'synced examples from ../windy/examples/'"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "windy-lang": "^2.2.0"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
